CMAKE_MINIMUM_REQUIRED(VERSION 2.6)
PROJECT(clglPlayer)
SET(SRCS main.c opengl.c opencl.c algorithm.c png.c shader.c)
SET(EXEC clglPlayer)
SET(PNG_LIB png)
#SET(OPENGL_LIB GL glut GLU GLEW)
SET(OPENGL_LIB GL glut GLU)
SET(OPENCL_LIB OpenCL)
set(CMAKE_C_FLAGS "${CMAKE_C_FLAGS} -ggdb")
ADD_EXECUTABLE(${EXEC} ${SRCS})
#ADD_DEFINITIONS(-DCL_SOURCE_DIR="${showImage_SOURCE_DIR}")
#message("source path: ${showImage_SOURCE_DIR}\n")
TARGET_LINK_LIBRARIES(${EXEC} ${PNG_LIB} ${OPENGL_LIB} ${OPENCL_LIB}) 
#ADD_LIBRARY(my SHARED ${hello_SRCS})
#ADD_LIBRARY(my ${hello_SRCS})
