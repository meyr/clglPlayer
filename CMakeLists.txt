CMAKE_MINIMUM_REQUIRED(VERSION 2.6)
PROJECT(showImage)
SET(SRCS showImage.c opengl.c)
SET(EXEC showImage)
SET(PNG_LIB png)
SET(OPENGL_LIB GL glut GLU GLEW)
ADD_EXECUTABLE(${EXEC} ${SRCS})
TARGET_LINK_LIBRARIES(${EXEC} ${PNG_LIB} ${OPENGL_LIB}) 
#ADD_LIBRARY(my SHARED ${hello_SRCS})
#ADD_LIBRARY(my ${hello_SRCS})
